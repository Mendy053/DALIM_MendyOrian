import { Button, Box } from "@mui/material";

const AddHTEvent: React.FC = () => {
    return (
        <>
            <Box marginTop={6}>
                <Button variant="contained" size="large">הוספת הפסק טהרה </Button>
            </Box>
        </>
    );
};

export default AddHTEvent;